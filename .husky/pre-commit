if git diff --cached --name-only | grep -q '^backend/'; then
	
	echo ""
	echo "Running consolidator tests..."
	dotnet test backend/consolidator || exit 1

	echo ""
	echo "Running generator tests..."
	dotnet test backend/generator || exit 1
fi
